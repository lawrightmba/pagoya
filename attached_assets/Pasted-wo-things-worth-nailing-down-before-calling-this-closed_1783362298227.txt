wo things worth nailing down before calling this closed, though — one is a genuine question, not a formality:
The portable_no_wallet_rail exclusion list — what exactly are the "4 intentionally-omitted keys"? If those four are the pre-existing wallet-rail-key fields (the original reason this fixture is called "no_wallet_rail"), that's the fixture behaving exactly as designed and the test fix is correct. But if the exclusion list accidentally includes any of the four new v4.3 derived-feature fields, that would mean this specific fixture has a permanent blind spot for exactly the fields this whole exercise was built to protect — the completeness test would report 14/14 green while silently never checking v4.3 fields on this one site. Given this fixture is precisely where an indirect/fragile reference was already flagged once (it inherits fields transitively via ...zeroDataSnapshot), I'd want the exclusion list named explicitly before treating this as resolved.
The import.meta.url guard added to the three scripts — this wasn't part of the gap-closure prompt, so it's a reasonable engineering call (importing toSnapshot() for testing would otherwise trigger main()), but it's an unrequested behavioral change to scripts that presumably still get run standalone in production/CI (ptiStressTest.ts, ptiAblationStudy.ts). Worth a one-line confirmation that each script still runs correctly via its normal CLI invocation after this guard was added — a quick "ran node ptiStressTest.ts, it executed main() and produced output" is enough.
Everything else in this report — the compiler-enforced PTI_DATA_SNAPSHOT_FIELD_MAP, the 185-passed count matching 171+14 exactly, the unchanged 7 pre-existing billpay.test.ts failures, the comment-only git diff — checks out internally and matches expectations cleanly.
If those two points come back clean, Stage 1 is genuinely done and you're clear to run Prompt 1.5 (balance reconstruction) next — the highest-risk remaining piece, with the SIGNUP_BONUS and spei_in pending→confirmed tests as the load-bearing proof once it's built.