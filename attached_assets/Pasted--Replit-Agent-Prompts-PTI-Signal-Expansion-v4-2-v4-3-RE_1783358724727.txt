# Replit Agent Prompts — PTI Signal Expansion (v4.2 → v4.3) — REV 7
### Corrects REV 6's incomplete status vocabulary. This is the last discovery item. No code changed yet.

**Changes from REV 6:**
1. **Real bug fixed — REV 6's `status='confirmed'` filter would have dropped signup bonuses.** `wallet_transactions.type` is plain `text()` with no central enum, populated across 13 write sites. Full vocabulary: 9 types (`load_oxxo`, `load_card`, `spei_in`, `load_banco`, `admin_credit`, `SIGNUP_BONUS` — uppercase, non-standard — `transfer_receive` as credits; `bill_pay`, `transfer_send` as debits) and 4 statuses (`pending`, `confirmed`, `failed`, `completed`). `signupBonusService.ts` writes the $150 signup bonus with `status='completed'`, not `'confirmed'`. REV 6's filter would have excluded real bonus money from balance reconstruction for essentially every user, understating `minBalanceBuffer30d` and falsely inflating `daysAtZeroPerMonth` — the same silent-plausible-wrong-numbers failure Stage 1.5 exists to prevent, reintroduced by its own fix.
2. **Filter corrected to a status classification, not a literal match:** balance-real = `{confirmed, completed}`, not-balance-real = `{pending, failed}`, implemented as an explicit per-status classification that throws on any unrecognized status — mirroring the type-classification guard, so a future status literal can't silently fall through either side.
3. **Ordering corrected to `COALESCE(confirmed_at, created_at)`, applied uniformly** — not `confirmed_at` alone, and not a per-type branch. `SIGNUP_BONUS`/`spei_in`/`load_banco` are written without `confirmed_at`; `bill_pay`/`transfer_send`/`transfer_receive`/`admin_credit` set it at write time. `COALESCE` handles both cases with one rule, which also resolves the debit-timestamp asymmetry REV 6 had left as an open per-type question — that hedge is removed.
4. **RESOLVED — spei_in/load_banco lifecycle confirmed by code trace, no escalation needed.** Both types are inserted `pending` and immediately transitioned to `confirmed` (with `confirmed_at` set) by `creditWallet()` (`wallet/services/wallet.ts:44`), called in the same request handler right after insert — `spei_in` via `stpWebhook.ts:217`, `load_banco` via `belvo-connect.ts`. SPEI/bank-load money is fully visible to balance features; there is no hidden funding-rail bug. The only rows with a *persistent* `pending` state are OXXO vouchers (awaiting real-world cash) and card intents (awaiting webhook) — both confirm later via the same `creditWallet` path and are correctly excluded until they do.
5. **Consequence of #4, sharpening the test plan:** because every load type gets `confirmed_at` set at the moment it becomes balance-real, `SIGNUP_BONUS` (written `status='completed'` directly, no `creditWallet` call, `confirmed_at` stays null) is the ONLY type in the balance-real row set with a null `confirmed_at`. It is therefore not just one edge case among several — it is the sole row type that exercises the `COALESCE(confirmed_at, created_at)` fallback at all. Its dedicated unit test is the single piece of evidence that the fallback ordering logic works, not just that the filter includes the right rows.

**Execution order: 0 → 0.5 → 1 → 1.5 → 2 → 3 → 4. Discovery is fully closed — no open placeholders, no pending pre-flights. REV 7 is handoff-ready as written below.**

---

## PROMPT 1.5 — Balance reconstruction helper (REV 7 — corrected vocabulary, the final fix)

```
CONTEXT: wallet_transactions has no balance-after column and no signed-amount column (amount_mxn is unsigned). type is plain text() with NO central enum — it is populated across 13 write sites scattered through the codebase, so the vocabulary must be enumerated from those sites directly, not assumed from a schema/type definition.

STATUS/TYPE GROUND TRUTH (confirmed from all 13 non-test write sites):

TYPES (9, no central enum):
- Credits: load_oxxo (pending→confirmed, OXXO cash), load_card (pending→confirmed, card top-up), spei_in (pending→confirmed via creditWallet() in stpWebhook.ts, STP/SPEI webhook), load_banco (pending→confirmed via creditWallet() in belvo-connect.ts, written as any, Belvo — deprioritized integration), admin_credit (confirmed, manual admin action), SIGNUP_BONUS (uppercase — non-standard casing, written with status='completed' by signupBonusService.ts, no creditWallet() call), transfer_receive (confirmed, P2P in).
- Debits: bill_pay (confirmed, wallet-funded bill payment), transfer_send (confirmed, P2P out).

STATUSES (4, not 3): pending, confirmed, failed, completed.
Balance-real states: {confirmed, completed}. NOT confirmed alone — SIGNUP_BONUS uses completed, and a confirmed-only filter silently drops all signup bonus money, which is a core product mechanic essentially every user has.
Not-balance-real states: {pending, failed}.

PRE-FLIGHT (resolved during planning — confirm still holds, do not re-derive from scratch):
1. Enumerate every wallet_transactions write site in the codebase (expect ~13, cross-check against the list above — if you find more or fewer, reconcile the discrepancy explicitly rather than silently trusting either count).
2. RESOLVED: spei_in (stpWebhook.ts:217) and load_banco (belvo-connect.ts) are both inserted pending and immediately transitioned to confirmed with confirmed_at set, via creditWallet() (wallet/services/wallet.ts:44) called in the same request handler right after insert. Confirm this still holds in the current codebase; if a code change has altered this path, treat it as a regression to flag, not a new discovery to re-investigate from zero.
3. Confirmed_at nullability per type, resolved: admin_credit, transfer_receive, bill_pay, transfer_send set confirmed_at at write time. load_oxxo/load_card/spei_in/load_banco are confirmed_at-null only transiently while pending, then get it set by creditWallet() upon confirmation — so among rows that pass the balance-real filter, these are NOT null. SIGNUP_BONUS is the sole exception: written directly with status='completed' and no creditWallet() call, so it remains confirmed_at-null even after passing the balance-real filter. This makes SIGNUP_BONUS the only row type that will ever exercise the COALESCE(confirmed_at, created_at) fallback in step 3(b) below — treat its unit test as the load-bearing proof of that logic, not just one case among many.

TASK — new module walletBalanceReconstruction.ts:
1. Type classification: explicit credit/debit mapping for all 9 confirmed types (including the uppercase SIGNUP_BONUS and the as-any load_banco). Throw on any type not in this set.
2. Status classification: explicit balance-real/not-balance-real mapping — balance-real = {confirmed, completed}, not-balance-real = {pending, failed}. Throw on any status not in this set (mirrors the type guard; a future status literal must not silently fall through either bucket).
3. reconstructBalanceSeries(userTransactions):
   a. Filter to rows where status classifies as balance-real. Exclude pending/failed entirely, regardless of type.
   b. Order the remaining rows by COALESCE(confirmed_at, created_at), applied uniformly across all types — no per-type branch. This one rule correctly orders both confirmed_at-bearing rows (bill_pay, transfer_send, transfer_receive, admin_credit) and confirmed_at-null rows (SIGNUP_BONUS, and any load_* type once it reaches a balance-real status without a populated confirmed_at).
   c. Return a running-balance series as {timestamp, balance} points, using the resolved COALESCE timestamp.
   d. Document the opening-balance assumption as a named constant.
4. toComparableTimestamp(): normalizes bill_payments.created_at (naive) to the tz assumption used by wallet_transactions, for cross-table comparisons.
5. Unit tests, covering ALL of the following:
   - single transaction
   - same-timestamp transactions (order ambiguity)
   - a full month of interleaved loads/spends (synthetic, covering multiple types)
   - an unrecognized type (must throw)
   - an unrecognized status (must throw)
   - zero transactions (empty series, not error)
   - a pending-then-confirmed load: balance rises at confirmed_at, not created_at, and doesn't appear while pending
   - a failed load: never affects balance
   - a SIGNUP_BONUS row (status='completed', confirmed_at=null): must be INCLUDED in the balance series, ordered by created_at via the COALESCE fallback, and must count as credit — this is the specific case that was wrong in REV 6, is the ONLY row type that exercises the COALESCE fallback at all (per the resolved pre-flight above), and must have a dedicated test proving it's now right. Treat this as the load-bearing test of the entire ordering logic, not one case among many.
   - a spei_in row that transitions pending → confirmed within the same synthetic sequence, verifying it's excluded while pending and included (with its confirmed_at, not created_at) once confirmed — confirming the resolved pre-flight's finding holds in the implementation, not just in the trace.
   - a mix of confirmed and completed rows for the same user, verifying both count as balance-real
6. Do NOT wire into PTIDataSnapshot yet.

GUARDRAILS: no dependency on pti.ts. Pure functions, fully unit tested, no live DB writes.

DEFINITION OF DONE: type classification covers all 9 confirmed types; status classification uses {confirmed, completed} as balance-real, not confirmed alone; ordering uses COALESCE(confirmed_at, created_at) uniformly with no per-type branch; the SIGNUP_BONUS test explicitly passes and is understood as the sole proof of the COALESCE fallback, not one case among several; the spei_in pending→confirmed test passes, confirming the resolved lifecycle finding holds in code; unrecognized type AND unrecognized status both throw.
```

---

## Prompt 1, Prompt 2 Part A reference — one wording update

Prompt 1 is unchanged from REV 6. Prompt 2 Part A's parenthetical should read:

```
(uses reconstructBalanceSeries — balance-real filtering (status IN {confirmed, completed}, not confirmed alone) and COALESCE(confirmed_at, created_at) ordering are handled INSIDE the Stage 1.5 helper; do not re-filter or re-order in this stage)
```

replacing REV 6's version, which still said "filter to status='confirmed'" in spirit even where it deferred to the helper — worth making the corrected rule explicit here too, so anyone reading Prompt 2 in isolation doesn't reconstruct the wrong assumption.

---

## Prompt 1 — fixture inventory correction (from Prompt 0 production verification)

REV 6 said "Prompt 1 unchanged from REV 5," and REV 5/REV 4 both listed **eleven** fixture locations, including `services/fairLendingAdjustment.ts` as a non-test snapshot-construction site. Prompt 0's fresh codebase scan found that file does NOT construct or assert on `PTIDataSnapshot` at all — it operates exclusively on its own isolated `FairLendingSnapshot` type (containing `colonia`/`declaredIncomeBucket`), by design, consistent with the isolation boundary the tests enforce. **The authoritative fixture count is 9 files / ~12 construction sites, not 11.** REV 6/REV 5/REV 4's inventory overcounted by one.

More importantly, Prompt 0 surfaced the actual mechanism behind the prior silent-divergence bug, which changes this from "update some fixtures" to "update this one site correctly or nothing else matters":

**`REQUIRED_SNAPSHOT_DEFAULTS` (`services/licenseeApi.ts:317`) is not just a typed fixture — it drives runtime behavior.** `licenseeApi.ts:344-345` builds every outgoing licensee payload by iterating `Object.keys(REQUIRED_SNAPSHOT_DEFAULTS)`. A field added to the `PTIDataSnapshot` type but not to this constant is **silently omitted from every licensee-facing payload at runtime** — no compiler error, no test failure, just a missing field quietly scoring at its default. This is almost certainly the actual cause of the prior version-bump divergence, and it is the single highest-risk site in this entire plan.

Two additional sites bypass the compiler and will not be caught by a type error if missed:
- `services/licenseeSandboxFixtures.ts:80` — `... as unknown as PTIDataSnapshot` (a cast, not a typed literal)
- `scripts/fairLendingClampStressTest.ts:77` — `... as PTIDataSnapshot` (same issue)

The remaining 7 sites are compiler-caught (typed literals or typed return values) and are self-defending — forgetting one fails the build, not the runtime.

**Corrected fixture inventory for Prompt 1 Part C (replaces the eleven-location list in REV 6/5/4):**

Runtime/service code:
1. `services/licenseeApi.ts:317` — `REQUIRED_SNAPSHOT_DEFAULTS` — **⚠️ runtime-silent, highest priority.** Verify with an explicit test asserting `Object.keys(REQUIRED_SNAPSHOT_DEFAULTS)` matches the full `PTIDataSnapshot` field set, not just that the constant compiles.
2. `services/licenseeSandboxFixtures.ts:19` — `zeroDataSnapshot: PTIDataSnapshot` (compiler-caught)
3. `services/licenseeSandboxFixtures.ts:80` — `... as unknown as PTIDataSnapshot` — **🔴 cast, silent, update by hand**
4. `services/syntheticPopulation.ts:185,266,281,292` — `synthFromLatents` / `coldBaseline` / `buildColdStart` / `buildContradictory` (compiler-caught)

Dev/analysis scripts:
5. `scripts/ptiAblationStudy.ts:121` — `toSnapshot(): PTIDataSnapshot` (compiler-caught)
6. `scripts/ptiStressTest.ts:116` — `toSnapshot(): PTIDataSnapshot` (compiler-caught)
7. `scripts/fairLendingClampStressTest.ts:77` — `... as PTIDataSnapshot` — **🔴 cast, silent, update by hand**

Test fixtures (each file has its own `baseSnapshot` helper — no shared factory):
8. `services/tests/pti.test.ts:10` — `baseSnapshot(): PTIDataSnapshot` (compiler-caught)
9. `services/tests/licenseeApi.test.ts:11` — `baseSnapshot(): PTIDataSnapshot` (compiler-caught)
10. `services/tests/fairLendingAdjustment.test.ts:114` — `baseSnapshot(): PTIDataSnapshot` (compiler-caught) — note: this is the ONLY fair-lending-related location in the inventory; `fairLendingAdjustment.ts` itself is confirmed NOT in scope.

**`services/fairLendingAdjustment.ts` (non-test) is explicitly OUT of the fixture inventory** — confirmed by direct code read to operate only on `FairLendingSnapshot`, never `PTIDataSnapshot`. Do not update it as part of any snapshot-shape change; doing so would violate the isolation boundary the tests exist to enforce.

**Amendment to Prompt 1's Part C / Definition of Done:** replace "update ALL ELEVEN fixture locations" with "update all 9 files / ~12 sites in the corrected inventory above." The schema-completeness test must specifically assert `Object.keys(REQUIRED_SNAPSHOT_DEFAULTS)` is a superset of every `PTIDataSnapshot` field (site #1) as a dedicated, separately-named test case — not just bundled into a generic "all fixtures match" check — given this is the specific site suspected of causing the prior divergence. The two cast sites (#3, #7) should also get a comment flagging them as compiler-invisible, so a future editor doesn't assume TS has their back.

Isolation-guard note (no change needed, confirmed by test run): `vitest run pti.test.ts licenseeApi.test.ts -t "isolation"` passes on current main (2 files, 6 tests). Forbidden patterns are exactly as REV 6/7 documented — `colonia`, `declared_income_bucket`/`declaredIncomeBucket` at the source level in `pti.ts`; import/call-site patterns for `fairLendingAdjustment` in `licenseeApi.ts`. `walletBalanceReconstruction.ts`'s planned naming is clear of all of them.

---



## Sequencing notes — final

- **This was the fourth distinct silent-corruption bug caught before any Stage 1.5 code was written**, and the second one specifically in balance reconstruction. That's not a sign the plan was fragile — it's what happens when every write site actually gets read instead of inferred from a schema comment. `type` being plain `text()` with no central enum across 13 scattered sites is exactly the kind of codebase condition where "the obvious filter" is reliably wrong.
- **The spei_in/load_banco pre-flight is now resolved, cleanly, with no product bug found.** Both types transition pending→confirmed synchronously via `creditWallet()` in the same request handler. SPEI money is visible to balance features.
- **The fixture inventory is corrected to 9 files / ~12 sites (not 11), and the actual failure mechanism is now named:** `REQUIRED_SNAPSHOT_DEFAULTS`'s runtime `Object.keys()` iteration in `licenseeApi.ts:344-345`, not a generic "fixtures might drift" risk. This is the single highest-priority site in Prompt 1 — get the dedicated superset-assertion test right there and the rest of the fixture work is largely mechanical. `services/fairLendingAdjustment.ts` is confirmed out of scope by design.
- **Once Prompt 1.5's SIGNUP_BONUS unit test passes, that's the concrete proof this bug class is closed** — and per the resolved pre-flight, it's now understood to be the *only* test exercising the COALESCE fallback at all, not one of several. If that test is weak or missing, the fallback ordering logic has effectively zero coverage; if it passes, the ordering logic is fully proven for every case the helper will actually see.
- **Discovery is now genuinely closed.** Run Prompt 1 with the corrected 9-file fixture inventory above, then Prompt 1.5 as specified, then proceed through Stage 2.