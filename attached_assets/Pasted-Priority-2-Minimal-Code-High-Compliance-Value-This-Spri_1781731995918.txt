Priority 2 — Minimal Code, High Compliance Value (This Sprint)
2A. Add KYC tier field to the users table
One column: kyc_tier — values: simplified, standard, enhanced. Default all current users to simplified. Write the classification logic as a database migration and a 20-line service function — no UI changes, no frontend changes. The tier assignment rule is simple: anyone under $3,200 MXN cumulative wallet activity = simplified. Anyone over = flag for standard (CURP collection). This gives PagoYa a documented, Ley Fintech-aligned user classification that you can show to any compliance officer. The PTI scoring cron already runs nightly — add kyc_tier to the user_financial_snapshots output at the same time.
2B. Add institutional_data_export endpoint to the admin API
A single new admin-only endpoint — GET /api/admin/compliance-summary — that returns an aggregate JSON report: total users by KYC tier, total transactions by period, average transaction value, fraud flags count, PTI tier distribution (Bronce/Plata/Oro), user retention cohort summary. No new data collection — this is entirely built from tables that already exist. The output becomes your live compliance dashboard for institutional conversations. Print it as a PDF before any partner meeting.
2C. Add referred_by_institution field to users table
One nullable column. When a SOFIPO pilot eventually sends their members to PagoYa, you need to attribute those users to the partner. Building the attribution column now — before the pilot — means the first pilot can start immediately without a code sprint to support it. This is the same pattern as the landlord referral code system just built, applied to institutional partners. Two-line migration.

Priority 3 — Paula Enhancements (One Targeted Prompt, No Architecture Change)
3A. Add a CURP collection flow to Paula
Paula already handles tier-differentiated messaging. Add one new conversation branch: when a user's cumulative transaction volume approaches the Tier 1 ceiling ($3,200 MXN), Paula sends a WhatsApp message requesting CURP. Conversational, frictionless, in character. The CURP field already exists in the codebase. This is a Paula prompt update and a single trigger condition — not an architecture change. It closes the KYC gap that is currently PagoYa's weakest compliance point and does it through a channel users already trust.
3B. Update Paula's tone documentation to include "financial identity building" language
Paula currently speaks to users about streaks, bonuses, and bill payment. Add one category of Paula responses specifically framed around financial identity — "Cada pago que haces construye tu historial financiero" — so that users naturally begin to understand the PTI as a credit-building instrument. No backend change. A prompt update. This also gives you authentic user-generated language about financial identity that you can quote in institutional presentations.

Priority 4 — Admin Dashboard Addition (One New Tab, Existing Data)
4A. Compliance tab in the command center
The command center already has tabs for various admin functions. Add one new tab — "CUMPLIMIENTO" — that displays the output of the compliance-summary endpoint from Priority 2B above in a formatted view: KYC tier distribution as a simple bar, PTI tier breakdown, fraud flags table, transaction volume by week, user retention curve. All existing data, new display only. This gives you a live compliance dashboard you can screen-share in any investor or institutional meeting. Build time: one focused session.

The Prioritized List, Ranked
#ActionCode ChangeTimeInstitutional Impact1Reframe PTI/confianza page copyNone — copy edit1 hourMedium — vocabulary alignment2Write AML/Compliance Policy docNone — document1 eveningVery High — due diligence eliminator3Add /cumplimiento page to siteMinor — new static page2 hoursHigh — first impression4Register with CONDUSEFNone — regulatory filing2–4 weeksVery High — official registry5KYC tier column + classification logicSmall — migration + service2–3 hoursVery High — closes biggest gap6compliance-summary admin endpointSmall — new endpoint2–3 hoursHigh — investor/partner dashboard7referred_by_institution columnTiny — one migration30 minsMedium — enables pilot attribution8Paula CURP collection flowMedium — prompt + trigger3–4 hoursHigh — completes KYC loop9Paula financial identity languageNone — prompt update1 hourMedium — narrative alignment10Compliance tab in command centerSmall — new tab, existing data3–4 hoursHigh — live demo asset